<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elliothannahiii.kotd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kotd_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kotd_logo"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MyKicksActivity" />
        <activity
            android:name=".ShoeNameActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".WeatherAddKicksActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".WhereToAddKicksActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".FavoriteShoeActivity"
            android:launchMode="singleTop" />
        <activity android:name=".ReviewNewShoeActivity" />
        <activity android:name=".ShoePictureActivity" />
        <activity
            android:name=".PickShoeActivity"
            android:launchMode="singleTop" />
        <activity android:name=".YourShoeForToday" />
        <activity android:name=".BarcodeOrManualActivity" />
        <activity android:name=".BarcodeActivity"></activity>
    </application>

</manifest>